{
  "_comment": "Registry of DGX-class compute hosts available for InfraScan workloads. Add new hosts here; selector reads this file at every invocation.",
  "hosts": [
    {
      "alias": "dgx-kail",
      "ssh_user": "kail",
      "ssh_key":  "/home/chan/.ssh/id_ed25519_dgx",
      "home":     "/home/kail",
      "gpu":      "GB10",
      "gpus":     1,
      "notes":    "First Spark box. Conda env at ~/miniconda3-envs-infrascan."
    },
    {
      "alias": "dgx-kangsan",
      "ssh_user": "chan",
      "ssh_key":  "/home/chan/.ssh/id_ed25519_dgx",
      "home":     "/home/chan",
      "gpu":      "GB10",
      "gpus":     1,
      "notes":    "Second Spark box. Conda env at ~/miniconda3-envs-infrascan."
    }
  ]
}
